DEFINE NOMBRE='LEANDRO '
DEFINE USU='LEANDROIANNINI'

DEFINE FWC= ((SUBSTR(NUM,1,3) = 'FWC') AND SUBSTR(NUM,4,5) IN (5))
DEFINE POL= ((SUBSTR(NUM,1,3) = 'POL') AND SUBSTR(NUM,4,5) IN (14))
DEFINE FRA= ((SUBSTR(NUM,1,3) = 'FRA') AND SUBSTR(NUM,4,5) IN (10,24))

DEFINE PAISES= (&FWC OR &POL OR &FRA)
